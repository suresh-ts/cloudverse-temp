Goal: Fix nav alignment + readability, make layout responsive across desktop/tablet/mobile, and wire every nav/CTA to match cloudverse.ai behavior. Keep Apple/“AIX” premium minimal style. Keep CloudVerse™ usage in eyebrow + page titles + first hero mention.

0) Hard requirements

Do not change the locked homepage hero copy except where specified below.

Ensure all pages look good at:

Mobile: 375px

Tablet: 768px

Desktop: 1280px+

No horizontal overflow at any breakpoint.

Use one consistent site container everywhere: max-w-[1240px] mx-auto px-5 sm:px-6 lg:px-8

Make spacing tighter: remove “giant dead space”, use consistent vertical rhythm.

Dark mode is primary. Ensure nav text is readable on dark background.

1) Update global navigation (SiteNav.tsx)

File: client/src/components/SiteNav.tsx

A) Align nav with page container

Wrap the inner nav content in the same container used everywhere:

div className="max-w-[1240px] mx-auto px-5 sm:px-6 lg:px-8"

Make nav height consistent:

h-16 lg:h-[72px]

Ensure it’s sticky and has a subtle background when scrolled:

default: transparent

scrolled: bg-black/60 backdrop-blur border-b border-white/10

B) Improve menu text visibility

Nav links should be brighter by default:

default: text-white/80

hover: text-white

active: text-white + subtle underline or pill

Avoid “muted-foreground” on dark; it’s too low contrast.

C) Mirror cloudverse.ai nav items
Use this order:

Platform → /platform

Solutions → /solutions

Integrations (label should be “Integration” or “Integrations” — keep as “Integrations” but map behavior)

Security → /security

Resources → /resources

Pricing → /pricing

Company → /company

Add: Partners → /partners (new page stub)

Add: Help → /help (new page stub)

D) Wire CTA buttons to production behavior

“Sign in” goes to external: https://id.cloudverse.ai (open in same tab is fine; external)

“Book a demo” goes to external: https://meetings.hubspot.com (open in new tab with rel="noopener noreferrer")

If you also have “Contact sales” anywhere, route it to /contact.

E) Add mobile nav (must)

At <lg, hide desktop links and show a hamburger icon button.

Clicking opens a right-side sheet/drawer (Apple settings style):

compact padding

links stacked

show Sign in + Book a demo actions at bottom

Use existing UI primitives if present (Sheet, etc). If not, implement a minimal drawer with portal + backdrop.

2) Fix hero section width + “cramped” feeling (Home.tsx)

File: client/src/pages/Home.tsx

A) Remove “boxed” layout constraints

Ensure the hero section uses the full container width and breathes:

pt-16 lg:pt-24

pb-10 lg:pb-16

Use a two-column layout on desktop, stacked on mobile:

grid grid-cols-1 lg:grid-cols-12 gap-10 lg:gap-12 items-center

Left: lg:col-span-6

Right: lg:col-span-6

B) Locked hero copy (use exactly)

Eyebrow: CloudVerse™

H1: Cloud financial management for modern enterprises.

Subhead: Visibility, allocation, anomalies, and automated optimization across cloud, data, and AI platforms.

Add one supporting line under subhead (smaller, calm):

Private deployment and air-gapped options available for regulated environments.

CTA primary: Book a demo → external hubspot link

CTA secondary: Watch 90-second tour → scroll to #tour section for now (anchor). Later we’ll swap to video modal.

C) Motion hero background (very subtle)
Implement a background layer behind hero:

A slow animated gradient haze (low opacity)

A faint grid pattern (1px lines, very low opacity)

Must not distract; keep it premium
Implementation guidance:

Use a div absolutely positioned with:

gradient blob via CSS animation (@keyframes)

grid via background-image: linear-gradient(...)

Respect reduced motion:

if prefers-reduced-motion, disable animations.

3) Replace the right hero “box” with a premium “Outcomes + Supported platforms” panel

Keep the Apple “window” style with 3 dots.

The right panel should be visually balanced, not empty.

Use a 2-column split inside the panel:

Left: “CloudVerse™ Outcomes” list (4–5 items max)

Right: “Supported platforms” icon grid + “AI & GPU providers” row

Add a slow crossfade between two outcome sets every ~6–8 seconds:

Set A: Visibility / Allocation / Anomalies / Reports

Set B: Automation / Tag Engine / Developer FinOps / Governance

Crossfade must be subtle: opacity transition only.

Supported platforms (don’t list everything in hero text, but icons are fine):

Clouds: AWS, Azure, GCP, OCI, Alibaba Cloud, Huawei Cloud, Tencent Cloud

Plus a small note: “Plus data, Kubernetes, and AI platforms”

AI & Data row: OpenAI, Databricks

Use SVGs/images already in client/public/assets if available; otherwise add placeholder monochrome icons temporarily.

4) Fix customer logos row spacing

Wherever the “Used by FinOps teams…” logo strip is:

Use equal visual sizing:

set max height: h-7 md:h-8

object-contain

reduce opacity slightly: opacity-80 hover:opacity-100 transition

Layout:

flex flex-wrap items-center justify-center gap-x-10 gap-y-6

Make sure it wraps nicely on mobile (2 per row or 3 per row).

5) Make every page responsive (global pass)

Apply these rules across pages:

Replace any hardcoded large vertical padding with:

section spacing: py-12 md:py-16 lg:py-20

All headings:

H1: text-4xl md:text-5xl lg:text-6xl font-semibold tracking-tight

H2: text-2xl md:text-3xl lg:text-4xl font-semibold tracking-tight

Body: text-base md:text-lg text-white/70 leading-relaxed

Avoid layouts that look “centered tiny island” on wide screens.

Use the max-w-[1240px] container and proper columns.

6) Wire all buttons + menu items (audit)

Create a small helper map in client/src/lib/links.ts:

SIGNIN_URL = "https://id.cloudverse.ai"

DEMO_URL = "https://meetings.hubspot.com"

internal routes: /platform, /solutions, /integrations, /security, /resources, /pricing, /company, /contact, /partners, /help

Then:

Replace any /demo or broken routes to correct ones.

Ensure every CTA has:

hover, focus ring, accessible labels

correct href

external links use target="_blank" rel="noopener noreferrer" where appropriate (Book a demo).

7) Deliverable

After changes:

Provide a quick checklist in chat:

“Nav aligned + readable”

“Mobile menu works”

“All CTAs wired”

“Hero motion background working”

“Hero panel crossfades”

“No overflow on mobile”

“Tablet layout looks intentional”

DO NOT add new heavy libraries. Keep it light.